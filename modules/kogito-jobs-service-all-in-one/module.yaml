schema_version: 1
name: org.kie.kogito.jobs.service.allinone
version: "2.0.0-snapshot"

envs:
  - name: "JOBS_SERVICE_FLAVOR"
    example: "mongodb"
    description: "The all in one image contains all the supported jdbc plugins for Jobs Service, ephemeral, infinispan, mongodb and infinispan, if empty, defaults to ephemeral"

execute:
  - script: configure
