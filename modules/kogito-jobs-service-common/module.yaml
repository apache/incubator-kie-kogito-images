schema_version: 1
name: org.kie.kogito.jobs.service.common
version: "2.0.0-snapshot"

envs:
  - name: "IMAGE_KIND"
    value: "all-in-one"
    description: "Used only for build time."

execute:
  - script: configure
